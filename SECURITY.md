# 安全边界

这是本地单用户工具。只运行你信任的项目和构建命令；它们拥有当前OS用户权限。

源码副本、常见密钥排除、最小环境变量、hash复核和超时回收不能替代安全沙箱。sandbox策略中的网络、只读、CPU/PID字段目前不强制执行；NODE_OPTIONS只限制Node堆，不能限制所有进程总内存。owner模式只是工具列表差异，不是权限隔离。

截图、DOM文字、日志和报告可能包含业务数据。数据默认保存在用户目录 `.leeway-ui-check`，可用HARNESS_HOME更改。发布反馈前请脱敏。报告服务绑定127.0.0.1，不应直接暴露公网。没有自动远程上传证据的产品流程；用户项目自身的网络请求不受隔离。

本项目未公布专用私密漏洞邮箱或响应时限。若仓库启用了私密漏洞报告，请使用该入口；否则先在issue中请求私密联系渠道，不公开密钥、敏感数据或可直接利用的细节。
